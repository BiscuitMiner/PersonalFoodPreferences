你是 PFP mod 架構師，嚴格遵守提供的 `AGENTS.md`。   
工作目標：在 `PersonalFoodPreferences` 模組中新增「Pawn 在社交互動時分享食物偏好」機制。  
先做**定點掃描**，不要全倉庫遍歷。

### 0 強制規則
1. 不修改任何原版檔案。  
2. 只可寫入：`G:\Rimworld\Mods\PersonalFoodPreferences\`。  
3. 遵守 SoC：Patch 薄層、邏輯進 Utility/Core 類、可配置值放 XML/Settings。  
4. 先確認原版類/方法存在再寫代碼。  
5. 回覆格式固定為：  
   - 定位  
   - 方案（含風險）  
   - 實作 + 驗證（含 SoC 合規檢查）

### 1 先掃描的核心社交檔案（只看這批）
請優先閱讀以下 RimWorld 原版 C#：

- `Source/RimWorld/Pawn_InteractionsTracker.cs`  
  - 目標方法：`TryInteractWith(...)`, `TryInteractRandomly(...)`
- `Source/RimWorld/InteractionDef.cs`
- `Source/RimWorld/InteractionWorker.cs`
- `Source/RimWorld/InteractionWorker_Chitchat.cs`
- `Source/RimWorld/InteractionWorker_DeepTalk.cs`
- `Source/RimWorld/InteractionWorker_KindWords.cs`
- `Source/Verse/PlayLogEntry_Interaction.cs`
- `Source/Verse/PlayLogEntry_InteractionSinglePawn.cs`

如果需要對話文本規則，再看 Def/XML 中與 `InteractionDef`、`RulePackDef` 相關檔案。  
除非證據不足，不要擴大掃描範圍。

### 2 本任務技術目標
我有 Pawn 的自訂「食物偏好」（例：`soup`）。  
我要在 A 與 B 社交時，A 能分享對該偏好的看法，並在日誌/文本中體現。  
要求：

- 優先方案：`XML + C#`  
  - XML：新增 `InteractionDef`、對話 rules（避免硬編碼文案）  
  - C#：自訂 `InteractionWorker`  
    - `RandomSelectionWeight(...)`：控制出現條件與權重  
    - `Interacted(...)`：讀取 A 的偏好，寫入互動效果（可含 thought/opinion/log grammar）
- 嚴禁在 C# 硬編碼 defName、可配置數值與字串鍵；這些搬到 XML 或 ModSettings。
- Patch 檔案保持薄層；複雜判斷放 Utility/Core 類。

### 3 交付物
1. 需要新增/修改的檔案清單（相對於模組根目錄）。  
2. 完整代碼（可直接落檔），至少包含：  
   - `Defs/InteractionDefs/*.xml`（新互動 Def）  
   - `Defs/RulePackDefs/*.xml`（對話文本規則）  
   - `Source/.../InteractionWorker_*.cs`  
   - 如需 Patch：`Source/.../Patch_*.cs`（≤10 行業務邏輯）  
   - Utility/Core 類（若有）  
3. 為每個設計決策附上「對應原版證據（檔案 + 方法名）」。  
4. 驗證清單：  
   - XML 結構與 XPath  
   - C# using、命名空間、Harmony 標記  
   - 1.6 可編譯性  
   - SoC 合規檢查（簡短）

### 4 限制與停機條件
- 若找不到關鍵方法或簽名不符：立即停止並回報差異，不要猜。  
- 若涉及未知的我方偏好資料結構：先提出最小接口假設，再等我確認。

### 5 補充
- 模組已更新至工坊，所以請先對方案進行分析，並告知能否兼容舊存檔，有沒有存在風險。